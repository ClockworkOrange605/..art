// TODO: better typization
type ObjectID = string
type URI = string

export { ObjectID, URI }

// TODO: better typization
type Address = string
type BlockHash = string
type TransactionHash = string

export { Address, BlockHash, TransactionHash }

// TODO: better typization
type BigNumber = string
type HexColor = string

export { BigNumber, HexColor }
